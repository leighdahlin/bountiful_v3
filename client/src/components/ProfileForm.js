

export default function ProfileForm ({ profileState, setProfileState, profileOnChange, profileSubmit }) {
    <form className="profile-submit">
        <input type="text" className="profile-input" name="first_name"></input>

    </form>
}